version: '3.9'

services:
  django:
    build: .
    command: bash -c "python manage.py migrate account admin auth contenttypes sessions socialaccount && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8080:8000"
    env_file:
      - .env
    container_name: django-builder
    restart: always

volumes:
  mysql_data:
